defmodule SecondBrainWeb.Api.V1.HistoryController do
  use SecondBrainWeb, :controller

  # alias SecondBrain.Brain

  # def index(conn, _params) do
  #   account_id = "account_id"

  #   with {:ok, history} <- Brain.get_work_session_history(account_id) do
  #     render(conn, "index.json", history: history)
  #   end
  # end
end
